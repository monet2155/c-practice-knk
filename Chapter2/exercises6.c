// Why is it not a good idea for an identifier to contain more than one adjacent underscore (as in current__balance, for example)?

// My Answer: Because it is useless...?
// Copilot's Answer: It is not a good idea for an identifier to contain more than one adjacent underscore because it is easy to confuse with a single underscore.